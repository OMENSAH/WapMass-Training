class Properties {
  // properties are like the features that describe our class
  // for example a class called car can have  name, color, type, manufacturer as properties
  
  // Defining our properties of class 
  // know the type of data. 
  // if a class has a property called name, it can be a String type. 
  
 // if a properties is defined with no value assigned it is giving a default value.
  String name;
  int age;
  double height;
  
  // defining and assigning values to properties
  String color  = "Blue";
  double weight = 15.8;
  
  public static void main(String[] args) {
//    System.out.println("Color: "  + color);
//    System.out.println("name: "  + name);
//    System.out.println("age: "  + age);
  }
}